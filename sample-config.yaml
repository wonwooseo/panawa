# fetch subcommand settings
fetch:
  codes:
    - "0000"

# backfill subcommand settings
backfill:
  codes:
    - "0000"
  # startdate: "2024-04-06"
  # enddate: "2024-04-23"

# data api info
api:
  kamis:
    url: "http://www.kamis.or.kr/service/price/xml.do?action=periodProductList"
    key: "111"
    id: "222"

# db connection info
mongodb:
  url: mongodb://admin:admin@localhost:27017
  database: panawa
